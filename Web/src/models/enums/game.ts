export enum ModOption {
    NONE = "None",
    SINGLE = "SinglePlayer",
    MULTI = "Muiltplayer",
}

export enum RoomStatus {
    OK = "OK",
    PROGRSS = "Progress",
    WAITING = "Waiting",
    FULL = "Full",
    STARTED = "AlreadyStarted",
    NOT_EXISTS = "NotExists",
}