export enum SessionKey {
    PLAYERS = "players",
    GAME_CARDS = "gameCards",
    FILTERS = "filters",
    BACKUP = "backup",
    ROOM = "room",
    ROUNDS = "rounds",
    CURRENT_PLAYER = "currentPlayer",


    WAR_ROOM = "warRoom",
}