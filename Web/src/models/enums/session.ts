export enum SessionKey {
    CURRENT_PLAYER = "currentPlayer",
    GAME_CARDS = "gameCards",
    GAME = "game",
    BACKUP = "backup",
    RIVAL_PLAYERS = "rivalPlayers",
    TIMER = "timer",
}

// WAR_ROOM = "warRoom",
// previewMovies