export enum SessionKey {
    CURRENT_PLAYER = "currentPlayer",
    GAME_CARDS = "gameCards",
    BACKUP = "backup",
    GAME = "game",
    ROUNDS = "rounds",
    ROOM = "room",
    FILTERS = "filters",

    WAR_ROOM = "warRoom",
}